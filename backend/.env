PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random
JWT_EXPIRES_IN=7d

# Database (we'll start with in-memory, later move to PostgreSQL)
DATABASE_URL=memory

# CORS
CORS_ORIGIN=*